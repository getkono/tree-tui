# Forward the commit hash exported by the release workflow into the cross build
# container (which has no `git`), so the cross-compiled Linux binaries report a
# real commit in `tree -V`. See `build.rs` and the Build step in
# `.github/workflows/release-plz.yml`.
[build.env]
passthrough = [
    "TREE_BUILD_SHA",
]
